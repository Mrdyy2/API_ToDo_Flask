import requests

# # URL de l'API météo (exemple)
# api_url = "http://127.0.0.1:5000/tasks"

# # Envoie une requête GET à l'API
# data = requests.post(api_url)

# # Vérifie si la requête a réussi (code 200)
# if response.status_code == 200:
#     data = response.json()  # Convertit la réponse en format JSON
#     print(data)  # Affiche les données météo
# else:
#     print(f"Erreur lors de la requête : {response.status_code}")
